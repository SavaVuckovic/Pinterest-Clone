<nav class="navbar">
  <a href="/"><img src="/images/logo.png" class="logo" alt="logo"></a>
  <div class="navbar-left">
    <a href="/" id="brand">Pinterest Clone</a>
  </div>
  {{#if loggedUser}}
    <div class="navbar-right">
      <div class="navbar-username">{{loggedUser.username}}</div>
      <img class="navbar-user-img" src="{{loggedUser.image}}" alt="user">
    </div>
  {{else}}
    <ul class="navbar-right">
      <li><span id="login-btn">Login</span></li>
      <li><span id="signup-btn">Sign Up</span></li>
    </ul>
  {{/if}}
  <div class="clearfix"></div>
</nav>

{{#if loggedUser}}
<div class="navbar-dropdown">
  <ul>
    <li><a href="/{{loggedUser.username}}">Profile</a></li>
    <li><a href="/auth/logout">logout</a></li>
  </ul>
</div>
{{/if}}
